# 核心依赖
requests==2.31.0
python-dotenv==1.0.0
schedule==1.2.0

# 飞书SDK
lark-oapi==1.36.0

# 标准库依赖（无需单独安装）
# json, base64, time, logging, threading, typing

# 可选依赖
# polymarket==0.1.0  # 用于量化交易（quant_trader.py）
